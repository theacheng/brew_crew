class CustomUser {
  final String uid;
  CustomUser({this.uid});
}

class UserData {
  final String uid, name, sugars;
  final int strength;
  UserData({this.uid, this.name, this.sugars, this.strength});
}
